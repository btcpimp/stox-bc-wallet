

module.exports = {
  cron: '*/5 * * * * *',
  job: async () => {
    // const {clientPromise, rpc} = createRpcConnection(mqConnectionUrl)
    // await clientPromise
    // const {headers, body} = await rpc('wallet-ABI', 'getABI', {address: 'here and there'})
    // console.log('wallet-issuing job request to wallet-ABI rpc queue. result:', body)
  },
}
